name: Version_Updator
on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      Component_Name:
        required: true
        type: string    
 
jobs:
   VersionUpdation: 
    runs-on: ubuntu-latest
    steps:
      - run: |
          ENV=$(echo ${{ github.event.inputs.Environment }} | tr '[:lower:]' '[:upper:]')
          echo $ENV is the environment
          echo $Component_Name is the component name

