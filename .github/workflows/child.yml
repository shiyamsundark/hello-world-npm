name: Child

on:
  workflow_call:

jobs: 
  Memory: 
    runs-on: ubuntu-latest
    steps:

  
     - run: |
        echo failing child job
        echo ${{ github.workflow }} is workflow name
        #exit 1
        
        

     
