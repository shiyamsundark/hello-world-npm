name: Memory

on:
  workflow_dispatch:

jobs: 
  Memory: 
    runs-on: ubuntu-latest
    steps:

     - run: |
        echo "------------------------free-----------------------"
        free -m
        echo "----------------------------------------------------------------"
        vmstat
        echo "----------------------------------------------------------------"
          
