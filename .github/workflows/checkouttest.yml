name: checkout_test
on:
  workflow_dispatch:

jobs:
  Test:
    runs-on: ubuntu-latest 
      
    steps:
      - name: Print the analysis branch
        run: echo ${{ github.head_ref }}
        

      - run: |
           echo "::error::Invalid UserStory..Please append UserStory number after AB#...Exiting!!"
           exit 1  
